apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arca-cms.fullname" . }}-test-connection"
  labels:
    {{- include "arca-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arca-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
