apiVersion: v1
kind: Service
metadata:
  name: {{ include "arca-cms.fullname" . }}
  labels:
    {{- include "arca-cms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arca-cms.selectorLabels" . | nindent 4 }}
